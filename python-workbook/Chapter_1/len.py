"""
The following example demonstrates the len function by
computing the length of a person’s name.
"""

# Read the name from the user
first = input("Enter yor first name: ")

# Compute its length
num_chars = len(first)

# Display the result
print("Your first name contains", num_chars, "characters")